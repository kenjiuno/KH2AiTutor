@page "/i/snez.s"

<RenderSetSOp NewName="snez.s"
              OldName="neqzf"
              CalcNewTP0="TP0 => ((TP0 != 0) ? 1 : 0)"
              Hint="set 1 if: not equal to 0"
              TP0="@TP0" />

@code {
    [SupplyParameterFromQuery(Name = "tp0")] float TP0 { get; set; }

    protected override void OnInitialized()
    {
        TP0 = (TP0 == default) ? 0 : TP0;
    }
}
