@page "/i/halt"

<PageTitle>halt (halt)</PageTitle>

<div class="card">
    <div class="card-header">halt instruction</div>
    <div class="card-body">
        No stack or memory changes occur.
    </div>
</div>

<div class="card mt-1">
    <div class="card-header">arguments</div>
    <div class="card-body">
        <div class="row">
            <div class="col-sm">1 of 4</div>
            <div class="col-sm">2 of 4</div>
            <div class="col-sm">3 of 4</div>
            <div class="col-sm">4 of 4</div>
        </div>
    </div>
</div>

<div class="card mt-1">
    <div class="card-header">example</div>
    <div class="card-body">
        <pre xml:space="preserve">; halt

halt ; exits the script with status BdStatus.Halt (1)

; (the following execution may occur)</pre>
    </div>
</div>

@code {
    protected override void OnInitialized()
    {
        Recalc();
    }

    private void Recalc()
    {
    }

    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            Recalc();
            StateHasChanged();
        }
    }
}
